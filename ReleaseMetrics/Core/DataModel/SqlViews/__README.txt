These files are NOT actually executed by anything. They are just stored here for reference.

Each of these corresponds to a view that is set up in a migration. Since migrations are deleted
when removed, the SQL is stored here in case a migration is deleted and the customizations disappear too.