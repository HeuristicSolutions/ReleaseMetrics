Classes in this folder are the data entities representing the underlying tables.

Take care exposing these to the web client as JSON or otherwise because as the 
data model grows, we might end up exposing a lot of unwanted and unnecessary stuff
to API calls.